# Frontend env example (copy to .env.local and fill values)
# NEXT_PUBLIC_* variables are exposed to the browser. Keep values safe.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Example if you host backend elsewhere:
# NEXT_PUBLIC_API_URL=https://api.example.com
